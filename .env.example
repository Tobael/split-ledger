# Domain for Traefik routing (set via GitHub secrets)
SPLITLEDGER_DOMAIN=expenses.example.com
SPLITLEDGER_RELAY_DOMAIN=relay.expenses.example.com
# Required by the project deployment workflow; keep this value secret.
RELAY_ADMIN_TOKEN=
